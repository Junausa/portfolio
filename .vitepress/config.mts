import { defineConfig } from 'vitepress'

// https://vitepress.dev/reference/site-config
export default defineConfig({
  title: "Juna",
  description: "Juna's portfolio site",
  base: "/juna-portfolio/"  //REMOVE THIS LINE WHEN DOMAIN IS READY
})
